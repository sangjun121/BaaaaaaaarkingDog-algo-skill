# Lecture Link Output Contract

For every lecture link, output the following sections.

## 오늘 배울 것

Keep this to one short paragraph.

## 이 유형을 알아보는 신호

List concrete problem-statement signals.

## 핵심 아이디어

Explain intuition before code.

## 구현 패턴

Give compact pseudocode or a Java skeleton when useful.

Include only the pattern the user needs now. Avoid dumping every variant.

## Java 암기 포인트

List Java syntax, library calls, and implementation idioms worth memorizing for this topic.

Examples:

- `ArrayDeque<int[]> q = new ArrayDeque<>();`
- `StringTokenizer st = new StringTokenizer(br.readLine());`
- `Arrays.sort(arr);`
- `PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> a[0] - b[0]);`

## 실수 포인트

List bugs that commonly cause WA or TLE.

## 문제 풀이 순서

Divide problems into:

- 필수: solve before moving on.
- 보강: solve if implementation is weak.
- 심화: solve only after basics are stable.

If the exact problem list is unavailable from the page, say so and ask for a workbook link or fetch it when tools allow.

## 오늘의 루틴

- 강의 읽기:
- Java 패턴 암기:
- 손코딩:
- 필수 문제:
- 오답 정리:
- 다음 단원 이동 조건:

## 이동 조건

State when the user can move to the next topic.
